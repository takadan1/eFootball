<div class="form-wrapper">
  <div class="form-container">
    <div class="form-header">
      <h1>試合記録を編集</h1>
      <p>内容を修正して、記録を更新してください。</p>
    </div>

    <%= render "form" do |f| %>
      <%# _form.html.erb の yield :form_actions にこのブロックが渡される %>
      <% content_for :form_actions do %>
        <%= link_to "詳細へ戻る", @match, class: "btn btn-secondary" %>
      <% end %>
    <% end %>
  </div>
</div>
